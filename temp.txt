# Medical Insurance Price Prediction

This project predicts medical insurance costs using a machine learning model. It features a user-friendly web interface built with Flask, where users can input their details (age, sex, BMI, etc.) and receive an estimated insurance price. The prediction model is an XGBoost Regressor trained on a dataset from Kaggle. The repository includes the full data analysis and model training process in a Jupyter Notebook (Medical_insurance_preice_prediction.ipynb).

![Screenshot of the application](https://drive.google.com/file/d/1m3Mj4DXNAgCuYWRSgxzcbPF0o9rheMaC/view?usp=sharing) <!-- Suggestion: Add a screenshot of your application -->

## Features

- **Insurance Price Prediction:** Utilizes an XGBoost model to predict insurance costs.
- **User-friendly Interface:** A clean and interactive web form to get predictions.
- **Informational Modals:** Provides details about medical insurance and the dataset used.
- **Responsive Design:** Adapts to different screen sizes.

## Technologies Used

- **Backend:** Flask (Python)
- **Frontend:** HTML, CSS, JavaScript
- **Machine Learning:**
    - XGBoost for the regression model.
    - Pandas and NumPy for data manipulation.
    - Scikit-learn for data preprocessing (assumed from standard practice).

## Dataset

The model was trained on the "Medical Insurance Cost" dataset, which is available on Kaggle. It is also included in this repository as `Medical_insurance.csv`. The dataset contains 1338 rows of data and the following columns:

-   `age`: Age of the primary beneficiary.
-   `sex`: gender of the beneficiary (female, male).
-   `bmi`: Body mass index.
-   `children`: Number of children covered by health insurance / Number of dependents.
-   `smoker`: Smoking status (yes, no).
-   `region`: The beneficiary's residential area in the US (northeast, southeast, southwest, northwest).
-   `charges`: Individual medical costs billed by health insurance.

## Model

The prediction model is an XGBoost Regressor. 
- The target variable (`charges`) was log-transformed (`log(x+1)`) to handle its skewness, a common practice for price prediction. 
- The categorical features (`sex`, `smoker`, `region`) were converted into a numerical format for the model.

For more details on Exploratory Data Analysis (EDA), feature engineering, and model evaluation, please refer to the `Insurance_price_prediction_K_10.ipynb` notebook.

## How to Run This Project Locally

1.  **Clone the repository:**
    ```bash
    git clone https://github.com/your-username/your-repository-name.git
    cd your-repository-name
    ```

2.  **Create and activate a virtual environment:**
    ```bash
    python -m venv venv
    # On Windows
    venv\Scripts\activate
    # On macOS/Linux
    source venv/bin/activate
    ```

3.  **Install the dependencies:**
    A `requirements.txt` file has not been provided. Based on the project files, you will need the following libraries. You can install them using pip:
    ```bash
    pip install Flask numpy pandas scikit-learn xgboost
    ```
    *(It would be best to create a `requirements.txt` file for easier setup.)*

4.  **Run the Flask application:**
    ```bash
    python app.py
    ```

5.  Open your web browser and go to `http://127.0.0.1:5000/` to use the application.

## Author & Contact

This project was created by Lalith Kumar Raju Somalaraju.

- **LinkedIn:** [lalith-kumar-raju-somalaraju](https://www.linkedin.com/in/lalith-kumar-raju-somalaraju/)
- **GitHub:** [lalith-kumar-raju](https://github.com/lalith-kumar-raju)
- **Email:** [ssivaprasadraju1978@gmail.com](mailto:ssivaprasadraju1978@gmail.com)

---

*Note: This README was partially generated by an AI assistant. As the assistant could not read the large Jupyter Notebook, some details about the model training and data analysis might be missing. Please review and add any necessary information from the notebook.* 